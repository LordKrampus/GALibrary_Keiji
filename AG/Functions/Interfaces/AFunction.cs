using System;

namespace GA.Functions.Interfaces
{
    /*
    public abstract class AFunction : IFunction
    {
        private bool _isMin;
        private double _factor;

        public bool IsMin => this._isMin;
        public double Factor => this._factor;

        public AFunction(bool isMin = false, double factor = 0f) 
        {
            this._isMin = isMin;
            this._factor = factor;
        }

        public abstract double Calc(object[] variables, bool applyFactor = true);

        protected abstract double Teste();
    } // end : class
    */

} // end: namespace
